function addToSession() {
  //get names and values from input elements.
  //pass that information to the php session variable.
  //need to add the session.php that will handle adding the information
}
